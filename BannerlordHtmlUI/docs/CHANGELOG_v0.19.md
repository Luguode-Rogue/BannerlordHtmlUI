# v0.20

- Consumer documentation no longer asks pages to include `runtime.js`; the framework injects it automatically.
- Added explicit request-handler threading guidance.
- Added a lightweight Node-based runtime protocol test under `tests/runtime-protocol.test.mjs`.
- Added a static packaging/test script for the repository.
