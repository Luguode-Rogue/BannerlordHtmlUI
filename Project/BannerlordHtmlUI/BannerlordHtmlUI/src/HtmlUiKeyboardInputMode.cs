namespace BannerlordHtmlUI
{
    /// <summary>Controls whether a page may consume keyboard focus when the host is Captured.</summary>
    public enum HtmlUiKeyboardInputMode
    {
        Captured = 0,
        Passthrough = 1
    }
}
