# v0.27

- Added `game.page` page context.
- Added consumer-scoped `game.scope().pages.open()` and `.close()`.
- Page navigation now carries owner and page metadata.
- Added TypeScript declarations for routing/context.
- Added routing documentation.
