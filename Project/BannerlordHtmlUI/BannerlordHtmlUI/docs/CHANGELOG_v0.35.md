# v0.35.0

## Framework maintenance

- Aligned the diagnostic `FrameworkVersion` with the module version.
- Added a version-consistency repository check.
- Updated the consumer example to use the canonical `game.app` API instead of manually prefixing owner names.
- Expanded the consumer example into the reference UI playground used for future framework acceptance tests.

## Compatibility

No new runtime protocol version was introduced. Protocol remains v1.
